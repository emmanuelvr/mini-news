import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    marginBottom: 5,
    color: '#000',
  },
  body: {
    fontSize: 18,
    color: 'rgba(0,0,0,.8)',
  },
});

export default styles;
